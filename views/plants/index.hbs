<div class="adminContainer">
	<h2>Current Inventory</h2>
	{{#each plantlist}}
		<div class="addy">
			<h5>{{this.plantname}}</h5>
				<a href="/plants/{{this.id}}/edit" class="btn btn-default btn-lg btn">Edit Details</a>
				<a href="/plants/{{this.id}}/view" class="btn btn-default btn-lg btn">View Plant</a>
		</div>
	{{/each}}
</div>